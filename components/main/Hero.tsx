import React from 'react'
import { default as HeroContent } from '../sub/HeroContent';

const Hero = () => {
  return (
    <section className="relative h-screen w-full overflow-hidden">
      <HeroContent />
    </section>
  )
}

export default Hero
